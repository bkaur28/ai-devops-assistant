pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Running build...'
                // Example: run tests
                // sh 'python -m unittest discover tests'
            }
        }
    }
}
